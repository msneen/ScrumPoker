@using ScrumPoker.Services;
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>@ViewBag.Title</title>
        <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
        <meta name="viewport" content="width=device-width" />
        @Styles.Render("~/Content/css")
        @Scripts.Render("~/bundles/modernizr")
    </head>
    <body>
        <header>
            <div class="content-wrapper">
                <div class="float-left">
                    <p class="site-title">@Html.ActionLink("Mike Sneen", "Index", "Home")</p>
                </div>
                <div class="float-right">
                    <section id="login">
                        @Html.Partial("_LoginPartial")
                    </section>
                    <nav>
                        <ul id="menu">
                            <li>@Html.ActionLink("Scrum Poker", "Vote", "Poker")</li><!-- This line is  a link to the first poker page -->
                            @if ( ScrumMasterSvc.IsScrumMaster(User.Identity.Name) )
                            {
                                <li><a href="#" id="aClearVotes">Clear Votes</a></li>
                                <li><a href="#" id="aClearAll">Clear All</a></li>
                            <li>@Html.ActionLink("Projects", "Index", "Project")</li>    
                            }
                            @if (SiteAdminSvc.IsSiteAdmin(User.Identity.Name))
                            {
                            <li>@Html.ActionLink("Administer Users", "Index", "UserManagement")</li>
                            }
                        </ul>
                    </nav>
                </div>
            </div>
        </header>
        <div id="body">
            @RenderSection("featured", required: false)
            <section class="content-wrapper main-content clear-fix">
                @RenderBody()
            </section>
        </div>
        <footer>
            <div class="content-wrapper">
                <div class="float-left">
                    <p>&copy; @DateTime.Now.Year - Mike Sneen</p>
                </div>
            </div>
        </footer>

        
        @Scripts.Render("~/bundles/jquery")
        @RenderSection("scripts", required: false)

        @Scripts.Render("~/bundles/scrumpoker")
    </body>
</html>
