@using ScrumPoker.Models;
@if (   "msneen".Equals(User.Identity.Name, StringComparison.OrdinalIgnoreCase)
            || "michael.sneen@gmail.com".Equals(User.Identity.Name, StringComparison.OrdinalIgnoreCase)
            || "michael.sneen.gmail.com".Equals(User.Identity.Name, StringComparison.OrdinalIgnoreCase) 
            || User.Identity.Name == "dle" 
            || User.Identity.Name == "jjohnston" 
            || User.Identity.Name == "rhanson" 
            || User.Identity.Name == "rodoughty")
{
    <hr />
    using (Html.BeginForm("AddUser", "Poker", FormMethod.Post))
    {
    <br /><br />
    <input type="text" id="userName" name="userName" /><input type="submit" value="Add User"/>
    <br />
    <select name="theUsers" onChange="combo(this, 'userName')">
        @foreach (User user in Users.UserList)
        {
        <option>@user.UserName</option>
        }
    </select> 
    
    }
}